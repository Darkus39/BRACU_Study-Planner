# 🛰️ BRACU Study Planner v2.0

> **[ 🖥️ CLICK HERE TO OPEN THE FULL INTERACTIVE README ](https://darkus39.github.io/BRACU_Study-Planner/)**
> *View the specialized Cyber-Mecha UI, installation guides, and architecture tree.*

---

### // ⚡ QUICK START (EXTRACTION)
The core automation scripts and application files are bundled in the repository's main directory. To begin:

1. **Download/Clone** the repository to your local machine (Fedora, Kali, or Windows).
2. **Locate the ZIP file** if you downloaded it as a bundle.
3. **Extract All Content** to a dedicated folder. 
   > ⚠️ *Crucial: The installers (`.sh` / `.bat`) must remain in the same directory as the `src` folder to function.*
4. **Run the Installer** corresponding to your OS (see the interactive link above for specific terminal commands).

---

### // 📂 REPOSITORY STRUCTURE
```text
StudyPlannerFX/
├── install-windows.bat   <-- Run this on Windows (Admin)
├── install-linux.sh     <-- Run this on Linux (chmod +x first)
├── install-macos.sh     <-- Run this on macOS
└── [ Core Project Files & Source ]
